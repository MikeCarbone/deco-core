{
	"name": "server",
	"version": "1.0.0",
	"description": "Server of deco plugins",
	"main": "index.js",
	"type": "module",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "nodemon index.js",
		"start": "npm run deploy && node index.js",
		"deploy": "node ./deploy.js"
	},
	"author": "Mike Carbone",
	"license": "MIT",
	"dependencies": {
		"express": "^4.18.2"
	},
	"devDependencies": {
		"nodemon": "^3.0.2"
	}
}
